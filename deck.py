colors = ['R', 'G', 'B', 'P']

cards = []
for c in colors:
    for n in range(1, 14):
        deck.append(c, str(n))
print(deck)

players = 2

decks_per_player = 5


decks = {}

for p in range(players):
    for d in range(decks_per_player:
        random.sample()
        deck

decks = {


}
